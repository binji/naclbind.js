var i = require('./index');

var files = ['add.c', 'glue.c'],
    outname = 'add',
    opts = {
      toolchain: 'pnacl',
      config: 'Release',
      outdir: '.',
      compile: {
        libs: ['ppapi'],
        args: ['-O2', '-pthread'],
      },
      createNmf: {}
    };

i.build(files, outname, opts, function(error, outfile) {
  console.log(error, outfile);
});
